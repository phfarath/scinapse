// Template. Copie para `Secrets.xcconfig` (gitignored) e preencha com o PUBLISH_SECRET
// configurado no Supabase (supabase secrets set PUBLISH_SECRET=...).
// É lido no Info.plist (chave PublishSecret) e injetado em Config.publishSecret no launch.
SCINAPSE_PUBLISH_SECRET =
